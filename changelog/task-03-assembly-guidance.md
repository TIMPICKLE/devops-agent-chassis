# T03 · 装配指导与可执行配方

承接 T01 的 `react_pattern()`，不重新实现其配置检查。按 skill-creator 的指导，主 Skill 保留关键决策，
ReAct 配方和证据接线放到按需读取的 reference，避免将每次个案扩写成通用硬性流程。

- 修复 reasoning 源码路径、NestedOrchestrator 示例参数语法及 ReWOO/LLMCompiler 能力描述。
- 更新 MCP 已实现状态、可选依赖及单连接串行限制；明确原生工具调用依赖、非流式边界和不存在自动兜底。
- 补齐并行接线、路径范围、独立验收、实际并发证据和自动步骤记账的说明。
- 新增可运行的统一装配与独立验收例子，明确 test-decider 标签；以行为测试验证单调用/并行、失败验收及证据一致性。
- Skill 变更随本仓库提交，不修改用户本机安装、业务载荷或企业配置。

测试与 CI 结果见本批总记录。示例不发起付费模型调用。
