# 项目引用规范 v1

编译器使用 C++17，并把项目根目录加入 include 搜索路径。
项目内头文件使用相对项目根目录的完整引用，例如 include/component/header.hpp。
遇到同名头文件时，读取内容，确认其提供了当前源文件需要的声明，不能只匹配文件名。
只能修改或补充 include 指令，保留其余代码；不要删除函数或修改返回值来绕过编译。
提交完整候选源码，由工具返回真实编译反馈。编译成功与独立范围检查通过才算完成。
